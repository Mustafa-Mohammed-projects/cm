name: secure_photo_vault
description: "Complete Zero-Trust Encrypted Camera & Vault App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+9
  encrypt: ^5.0.3
  flutter_secure_storage: ^9.0.0
  path_provider: ^2.1.2
  path: ^1.9.0
  flutter_windowmanager: ^0.2.0
  permission_handler: ^11.3.1
  archive: ^3.4.10
  image: ^4.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
